app:
  command: "node index.js"
  port: 3000
services:
  postgres:
    enabled: true
  redis:
    enabled: false
  mailpit:
    enabled: false
  minio:
    enabled: false
commands:
  install: npm install
  dev: npm run dev
  start: npm start
